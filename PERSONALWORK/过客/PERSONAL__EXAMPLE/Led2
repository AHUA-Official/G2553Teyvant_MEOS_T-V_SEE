#include <msp430.h> 


/**
 * main.c
 */
int main(void)
{
	WDTCTL = WDTPW | WDTHOLD;	// stop watchdog timer
	P1DIR |=BIT6;
//	初始化LED2所在IO口P1.6为低电平，LED初始状态为灭
	P1OUT &=~BIT6;
	while(1)
	{
	    P1OUT ^=BIT6;
	    __delay_cycles(50000);
//	    隔500ms闪一次
	}

	
	return 0;
}
